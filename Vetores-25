#include <stdio.h>
#include <stdlib.h>

int teste(int n) { 
  if (((n % 7) != 0) && (((n + 3) % 10) == 0) ) {
    return 1; 
    } else { 
    return 0; 
    }
  }

int main() {
int i, vetor[100];
for (i = 0; i <= 100; i++) { 
  if (teste(i)) { 
    printf("%d - ", i); 
    } 
  }

  return 0;
  }
